================================================================================
FUEL GUARD X - PORTS, LINKS & ACCESS
Last updated: 2026-07-26
VPS public IP : 31.42.125.144
Domain        : dashboard.aayunexinnovations.com  ->  31.42.125.144
--------------------------------------------------------------------------------
NOTE ON SECRETS: real secrets (DB password, Django SECRET_KEY) are NOT in this
file. They live in /root/erp/backend/.env (gitignored, mode 600). The dev login
passwords below are for the pilot only - CHANGE THEM BEFORE PRODUCTION.
================================================================================


PUBLIC LINKS (open in a browser)  --  ALL ERPs on ONE subdomain, HTTPS via Caddy
--------------------------------------------------------------------------------
  Owner / Company ERP  https://erp.aayunexinnovations.com/dealer/   login: owner1 / owner123
  Super Admin ERP      https://erp.aayunexinnovations.com/admin/    login: admin  / admin123
  Driver ERP (pilot)   https://erp.aayunexinnovations.com/pilot/    login: driver1 / (see creds file)
  Bare host            https://erp.aayunexinnovations.com/          -> redirects to /dealer/

  Django Admin panel   NOT publicly routed (as of 2026-08-02) -- no nginx location
     for /django-admin/ means no network path to it from the internet at all.
     Reach it via SSH tunnel:
       ssh -L 8000:127.0.0.1:8000 root@31.42.125.144
       then browse http://127.0.0.1:8000/django-admin/   login: admin / admin123
  Receiver Dashboard   https://dashboard.aayunexinnovations.com     login: admin / admin123
     (legacy device dashboard - separate subdomain, also HTTPS via Caddy)

  HOW IT ROUTES: Caddy terminates TLS for erp.aayunexinnovations.com and reverse-
  proxies to nginx :8090, which path-routes: /dealer/ /admin/ /pilot/ (SPAs),
  /api/ + /django-admin/ (Django). Direct http://31.42.125.144:8090/... still works
  for on-server testing but the public, TLS URLs above are the ones to use.


BACKEND API (public, through nginx)
--------------------------------------------------------------------------------
  Base                          https://erp.aayunexinnovations.com/api/
  Health check                  https://erp.aayunexinnovations.com/api/health  -> {"ok":true}
  Auth login (POST)             https://erp.aayunexinnovations.com/api/auth/login
     (One shared API behind all three ERP paths; same-origin, so SPAs call /api/... )


DEVICE TELEMETRY (HTTP ONLY - required, the SIM800L cannot do TLS)
--------------------------------------------------------------------------------
  Ingest URL   http://31.42.125.144/api/telemetry     (port 80, plain HTTP)
  Auth header  X-Auth: fuelguardx
  Served by Caddy -> old receiver on 127.0.0.1:8080 (legacy path, unchanged).
  New Django ingest also exists at :8090/api/telemetry for future cutover.


DATABASE (PRIVATE - no web link, and must stay that way)
--------------------------------------------------------------------------------
  Engine    PostgreSQL 16
  Host/Port 127.0.0.1 : 5432   (local-only; refuses public connections = correct)
  Database  fuelguardx
  User      fuelguardx
  Password  see DATABASE_URL in /root/erp/backend/.env
  Connect on the server:   su postgres -c "psql fuelguardx"
  A database is NOT a website - never expose 5432 to the internet.


INTERNAL SERVICES (127.0.0.1 = reachable only from inside the VPS)
--------------------------------------------------------------------------------
  Django API (gunicorn)   127.0.0.1:8000    <- nginx proxies to this
  PostgreSQL              127.0.0.1:5432
  Old receiver-dashboard  127.0.0.1:8080    <- Caddy proxies to this
  These are deliberately private; the public reaches them only via nginx/Caddy.


FULL PORT MAP
--------------------------------------------------------------------------------
  Port   Scope        Service                          Public link
  22     public       SSH                              ssh root@31.42.125.144
  80     public       Caddy (device HTTP + ACME)       http://31.42.125.144/api/telemetry
  443    public       Caddy (HTTPS: ERP + legacy dash) https://erp.aayunexinnovations.com
  8090   local->Caddy nginx -> all 3 ERPs + API        (public via erp.aayunex... over 443)
  8000   local-only   Django / gunicorn                (internal, via nginx)
  8080   local-only   old receiver-dashboard           (internal, via Caddy)
  5432   local-only   PostgreSQL                       (internal, private)
  2019   local-only   Caddy admin API                  (internal)
  NOTE: nginx :8091 retired - Super Admin now lives at erp.aayunex.../admin/ (path,
  not a port). :8092 no longer needed either; Driver ERP will be the /pilot/ path.


WHY 127.0.0.1 vs 31.42.125.144
--------------------------------------------------------------------------------
  Public traffic now enters ONLY on 443 (Caddy) and 80 (device + ACME). Caddy
  proxies erp.aayunexinnovations.com -> nginx :8090 -> gunicorn :8000. nginx and
  gunicorn are internal engine parts; the public never hits 8090/8000 directly.
  This is standard, secure architecture and needs no extra provider-firewall ports.


TLS / HTTPS  (DONE)
--------------------------------------------------------------------------------
  erp.aayunexinnovations.com -> A record 31.42.125.144 (IPv4 only; AAAA removed so
  Let's Encrypt validates against the VPS). Caddy auto-issued + auto-renews the cert.
  All ERP paths are HTTPS; http:// is 308-redirected to https:// by Caddy.
  Reachability caveat is resolved: no 8090/8091 provider-firewall ports required.


TEST / DEV LOGINS (pilot only - CHANGE BEFORE PRODUCTION)
--------------------------------------------------------------------------------
  The old weak passwords (admin123 / owner123) were ROTATED to strong random ones
  on 2026-07-26. Passwords are NOT stored in this committed file anymore.
  See the gitignored file (server only, mode 600):
      /root/erp/deploy/PILOT_CREDENTIALS.txt
  Users:  admin  -> Super Admin ERP + Django admin
          owner1 -> Owner ERP ("Aayunex Transport")
          driver1 -> Driver ERP (assigned to CG04-TEST-01 / esp32-01)
================================================================================
